# Diff 测试文档

这是一个用于测试 AI diff 修改能力的文档。每一节都有明确的标记，方便验证修改是否精准。

## Section A: 基础信息

- 项目名称：LeetCode Solutions
- 作者：LetMeFly666
- 创建日期：2026-04-19
- 版本号：v3.2.0
- 状态：Round 7c 单hunk验证完成

## Section B: 代码示例

```python
def hello_world():
    """一个简单的示例函数"""
    message = "Hello, World!"
    print(message)
    return message

def add(a, b):
    """两数相加"""
    result = a + b
    return result

def subtract(a, b):
    """两数相减"""
    result = a - b
    return result

def multiply(a, b):
    """两数相乘"""
    result = a * b
    return result
```

## Section C: 表格数据

| 编号 | 名称 | 难度 | 状态 |
|---|---|---|---|
| 001 | 两数之和 | 简单 | 已完成 |
| 002 | 两数相加 | 中等 | 已完成 |
| 003 | 无重复字符的最长子串 | 中等 | 已完成 |
| 004 | 寻找两个正序数组的中位数 | 困难 | 已完成 |
| 005 | 最长回文子串 | 中等 | 未开始 |
| 006 | 正则表达式匹配 | 困难 | 未开始 |

## Section D: 段落文本

这是第一个段落。它包含了一些普通的文本内容，用于测试在段落中间插入或修改文字的能力。这个段落足够长，可以验证模型是否能精确定位到段落内的特定位置。

这是第二个段落。与第一个段落相似，它也包含了一些普通文本。但这个段落的内容略有不同，以确保模型不会混淆两个段落。

这是第三个段落（已恢复）。这个段落会被用于测试删除操作——在后续的实验中，可能会尝试删除这整个段落或其中的部分内容。

## Section E: 嵌套列表

1. 第一大项
   - 子项 1.1
   - 子项 1.2
   - 子项 1.3
2. 第二大项
   - 子项 2.1
   - 子项 2.2
   - 子项 2.3
3. 第三大项
   - 子项 3.1
   - 子项 3.2
   - 子项 3.3
   - 子项 3.4

## Section F: 结尾标记

文档版本：v3.1.0
最后修改：2026-04-19 Round 7b
修改次数：8
校验值：CHECKSUM_R7B
