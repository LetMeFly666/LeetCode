---
title: Bot Diff方案探索 - 如何让LLM精确修改文件
date: 2026-04-19 17:46:00
tags: [技术探索, Bot, Diff, LLM, 代码修改]
categories: [技术探索]
---

# 【LetMeFly】Bot Diff方案探索 - 如何让LLM精确修改文件

## 问题背景

在使用LLM Bot进行代码审查和自动修改时，我们发现一个普遍性问题：

- **第一次修改（a→b）**：Bot表现良好，能准确创建或修改文件
- **后续修改（b→c）**：Bot容易"乱改"，修改位置不准确，甚至破坏原有内容

这个问题在多个模型上都有体现，即使是Claude等"地表最强"模型也不例外。

## 问题分析

### 1. 为什么第一次修改容易成功？

第一次修改时，Bot可以使用"全量文件内容"模式（Full File Content），直接提供完整的文件内容。这种方式简单直接，不容易出错。

### 2. 为什么后续修改容易失败？

后续修改时，文件已经存在且内容可能较长。此时有两个困难：

- **上下文窗口限制**：大文件内容经过LLM的上下文窗口后容易被截断、总结或产生Unicode错误
- **Diff定位不准确**：LLM生成的unified diff中，上下文行可能与实际文件内容不完全匹配
- **多处修改干扰**：需要同时修改多个位置时，hunk之间的行号计算容易出错

### 3. 常见的"乱改"表现

| 问题类型 | 具体表现 | 根因 |
|---------|---------|------|
| 位置偏移 | 修改了错误的行 | 行号计算错误 |
| 内容截断 | 文件末尾内容丢失 | 上下文窗口溢出 |
| 重复内容 | 同一段内容出现两次 | Hunk边界重叠 |
| 无关修改 | 修改了不应该改的地方 | 上下文匹配模糊 |

## 方案探索

### 方案一：精确定位法

**核心思想**：每次diff只修改一个位置，使用足够多的上下文行来确保精确定位。

```
--- a/file.md
+++ b/file.md
@@ -10,7 +10,7 @@
 这是第8行的内容
 这是第9行的内容
 这是第10行的内容
-这是需要修改的旧内容
+这是修改后的新内容
 这是第12行的内容
 这是第13行的内容
 这是第14行的内容
```

**优点**：定位准确，不容易出错
**缺点**：需要多次commit才能完成多处修改

### 方案二：分段Diff法

**核心思想**：将多处修改拆分为多个独立的hunk，每个hunk有充足的上下文。

关键要点：
1. 每个hunk至少包含3行上下文（前后各3行）
2. 相邻hunk之间至少间隔1行未修改内容
3. 行号必须精确计算，考虑前面hunk对行号的影响

### 方案三：锚点定位法

**核心思想**：利用文件中唯一性强的文本作为"锚点"来定位修改位置。

比如：
- 使用标题行（`## 某个标题`）作为锚点
- 使用函数签名作为锚点
- 使用特殊注释作为锚点

## 实验验证

以下是对各方案的实验记录。我们将在本文件上进行多次修改来验证效果。

### 实验1：基准测试

**状态**: 待执行

本次实验将验证最基本的单处修改是否能准确执行。

### 实验2：多处修改测试

**状态**: 待执行

本次实验将验证同时修改多个位置的准确性。

### 实验3：大文件修改测试

**状态**: 待执行

本次实验将在较长的文件上进行修改，验证大文件场景下的准确性。

## 结论与最佳实践

（待实验完成后总结）

> 本文由 @gh-pr-review bot 创建，用于探索和验证LLM进行精确文件修改的方法。
> 同步发文于CSDN，原创不易，转载请附上[原文链接](https://blog.letmefly.xyz/2026/04/19/Other-BotDiff%E6%96%B9%E6%A1%88%E6%8E%A2%E7%B4%A2/)哦~
