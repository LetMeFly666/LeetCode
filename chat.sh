我有一台服务器，我希望开发一个github pr review机器人，具有以下功能：
触发：
- 设置一个http trigger，监听一个地址，接收pr地址。当接收到这个参数时，先打印即可，后续再设置大模型调用流程。
- 监听到地址后，回复对应pr一个👀emoji（不是评论）
- 监听完地址后，生成一个key，想办法让我看到
- 必须是白名单的github pr地址才能review，否则不予响应。
- 可以要求用户授权设置pr监听地址。
响应LLM的review结果：
- 开放一个端口，
- 可能需要设置成一个MCP？怎样都行，也可以是给大模型一个SKILL，反正模型按规矩review完毕后可以直接调用这个接口，bot发送评论。
- 模型带上这个key才能响应模型操作
- review可能包括：整体评论、面向具体行的suggest

这可能包含服务器代码（最好用go实现，并且最好少用第三方依赖）、github 机器人设置，请详细告诉我方法。



我们先详细讨论这个需求，每一步你都要把需求记下来，最终讨论过后，你可以给我一个完整的设计文档。










1. GitHub 身份：GitHub App
2. 触发方式：纯http吧，我希望内存中维护一个简单的pr-key map，只要这个key还没有过期，就不会生成新的key（这是因为我有一个专门的api-hook-listener服务，github的webhook也hook到那个服务上，那个服务再转发给这个服务）
记得让我有办法看到这个pr对应的key列表，可以提供一个借口让我查看；这个列表还需要设置“手动添加pr”的功能；这个列表还能手动修改、删除等。
3. Review key 的生命周期：这个pr期间可以多次使用
4. 白名单粒度：具体仓库
5. LLM调用链路：每次生成一段固定的话，让我手动copy发给LLM。这可能要包含key、pr地址
bot也可以评论一个地址，是https，以便我可以方便地快速点进去查看review相关信息。

另外：设计简单的鉴权操作，无需支持多用户，哪怕是cookie中带上硬编码的密码都可以，我有nginx做转发，天然支持https加密（https中别人抓包或者网络提供商应该也看不到cookie内容吧）





---
算了，先实现基础功能，再慢慢追加吧




我有一个部署了nginx和https的服务器，我要设计一个github pr review机器人，具有以下基础功能：
1. 服务器提供一个http接口，接口接收一个github pr地址，监听到这个端口后，服务器控制机器人向这个pr回复一个👀emoji（不是评论）
2. 提供一个接口，接收review信息、pr地址，并控制机器人对这个pr地址做出对应review。
  - review支持 总体评论、对具体行的评论（以及可能包含的suggestion语法块）

先讨论下这个需求和设计






技术栈：Go，并且尽可能少使用第三方依赖库
接口鉴权：暂不设计
Review event 类型：调用方指定，若未指定则默认COMMENT
行级评论的输入格式：调用方自己组装吧





1. 不要使用简单的PAT，要使用向codex一样的独立bot，以便后续拓展。并告诉我怎么设置
2. 项目名叫gh-pr-review
3. 端口http://127.0.0.1:8566
