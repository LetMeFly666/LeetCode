class Solution {

    /**
     * @param Integer[] $robots
     * @param Integer[] $distance
     * @param Integer[] $walls
     * @return Integer
     */
    function maxWalls($robots, $distance, $walls) {
        
    }
}